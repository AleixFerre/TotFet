import 'package:flutter/material.dart';

class CrearLlista extends StatefulWidget {
  @override
  _CrearLlistaState createState() => _CrearLlistaState();
}

class _CrearLlistaState extends State<CrearLlista> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text("Crear una llista")),
      ),
      // TODO EL FORMULARI
      // es retorna un objecte llista amb els seus atributs
    );
  }
}
